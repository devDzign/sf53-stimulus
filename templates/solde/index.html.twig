{% extends 'base.html.twig' %}

{% block title %}Hello SoldeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper {
        margin: 1em auto;
        max-width: 800px;
        width: 95%;
        font: 18px/1.5 sans-serif;
    }

    .example-wrapper code {
        background: #F5F5F5;
        padding: 2px 6px;
    }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate', 'data-controller':'solde-form'}}) }}
    {{ form_row(form.data1) }}
    <div data-solde-form-target="toUpdate" data-depend-on="data1"
         data-update-url="{{ path('solde_data2') }}"
         data-action="refreshItem->solde-form#refreshitem">test div</div>
        {{ form_end(form, {"render_rest":false}) }}
    </div>
{% endblock %}
